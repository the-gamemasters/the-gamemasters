import React, { ReactElement } from 'react'

interface Props {
    
}

export default function Login({}: Props): ReactElement {

    return (
        <div>
            <h1>Login</h1>
            
        </div>
    )
}
